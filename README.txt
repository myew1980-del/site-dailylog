更新包內容：
1. index.html  —— 主頁（已引用外部 script.js、style.css）
2. script.js   —— 安全匯入程式（不使用 eval/new Function；支援 CSV/JSON）
3. style.css   —— 版面樣式

安裝方式：
A) 下載本 zip，解壓縮後把三個檔案拖到你的 GitHub repo（site-dailylog）的根目錄，覆蓋同名檔案，Commit 至 main。
B) 等 GitHub Pages 自動部署完成（約 1 分鐘），強制重整（Ctrl+F5）即可。
